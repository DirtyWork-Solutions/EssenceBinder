def will_fit_inside(item, to_fit_in, allow_exact: bool = False) -> bool:  # TODO: make it work with shapes (particularly dimensions)
    """

    :param item:
    :param to_fit_in:
    :param allow_exact: should X fit inside Y even if they are the exact same size? If false item must be some size smaller
    :return:
    """