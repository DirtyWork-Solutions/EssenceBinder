from typing import Optional

from essencebinder.commons.patterns.mixins import HasMetadataMixin

def HasMetadata(first_records: Optional[dict] = None):  # TODO: Create a metadata decorator
    """
    **Decorator** to add metadata to a class.

    :param first_records:
    :return:
    """
    pass
